<div class="d-flex flex-column gap-3">
  <h1>New List</h1>

  <%= form_with model: @list do |f| %>
    <%= f.text_field :name %>
    <%= f.submit %>
  <% end %>

  <%= link_to "Back to list", lists_path %>
</div>
